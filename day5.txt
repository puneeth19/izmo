create view emp as (select last_name,salary,hire_date from employees where manager_id is not null);
 select dname from emp1 where ename ='king';
 SHOW FULL TABLES IN izmo WHERE TABLE_TYPE LIKE 'VIEW';

 -- create view emp1 as (select e.last_name as ename,d.department_name as dname from employees e inner join departments d on e.department_id=d.department_id order by 1 desc);
-- drop view emp1;

-- create view emp2 as(select salary,commission_pct from employees); 
use izmo;
-- create table person(id int primary key,name varchar(20) not null,email varchar(30)unique);
-- insert into person values(2,null,'b@a.com'); 
-- insert into person(id,email,name) values(3,'C@c.com','ccc');
-- select * from person;
-- drop table person;
-- create table person(id int primary key,name varchar(20),age int not null check(age>=10 and age<=60) );
-- insert into person values(1,'aa',null);
-- create table person(id int primary key,name varchar(20),age int  check(length(age)<=2) );
-- insert into person values(1,'aa',100);
-- create table user (name varchar(20) check(substring(name,1,1)='A'));
--  insert into user values('aa');
-- drop table user
-- create table info(name varchar(20),phone int,email varchar(25),primary key(phone,email));
-- desc info;
-- create table dept(did int primary key,dname varchar(20));
 -- insert into dept values(1,'account');
-- insert into dept  values(2,'finance');
-- select * from dept;

-- create table empp(id int primary key auto_increment,name varchar(20),dept_id int,foreign key(dept_id) references dept(did));
-- insert into empp(name,dept_id) values('vikaas',1);
-- select * from empp;
-- desc empp;
-- drop table t1;
-- create table t1(name varchar(20),phone int,email varchar(20),primary key(phone,email));
-- insert into t1 values('C',124,'c@a.c');
-- select * from t1;
-- create table t2(id int,phone int,email varchar(20),foreign key(phone,email) references t1(phone,email)); 
-- insert into t2 values(12,133,'b@c');
 -- create table t3(id int, phone int,foreign key (phone) references t1(phone));
 -- insert into t3 values(1,134);
use izmo;
-- create table person(id int primary key,name varchar(20) not null,email varchar(30)unique);
-- insert into person values(2,null,'b@a.com'); 
-- insert into person(id,email,name) values(3,'C@c.com','ccc');
-- select * from person;
-- drop table person;
-- create table person(id int primary key,name varchar(20),age int not null check(age>=10 and age<=60) );
-- insert into person values(1,'aa',null);
-- create table person(id int primary key,name varchar(20),age int  check(length(age)<=2) );
-- insert into person values(1,'aa',100);
-- create table user (name varchar(20) check(substring(name,1,1)='A'));
--  insert into user values('aa');
-- drop table user
-- create table info(name varchar(20),phone int,email varchar(25),primary key(phone,email));
-- desc info;
-- create table dept(did int primary key,dname varchar(20));
 -- insert into dept values(1,'account');
-- insert into dept  values(2,'finance');
-- select * from dept;

-- create table empp(id int primary key auto_increment,name varchar(20),dept_id int,foreign key(dept_id) references dept(did));
-- insert into empp(name,dept_id) values('vikaas',1);
-- select * from empp;
-- desc empp;
-- drop table t1;
 -- create table t1(name varchar(20),phone int,email varchar(20),unique(phone,email), primary key(name));
-- insert into t1 values('C',124,'c@a.c');
-- select * from t1;
-- create table t2(id int,phone int,email varchar(20),foreign key(phone,email) references t1(phone,email)); 
-- insert into t2 values(12,133,'b@c');
 -- create table t3(id int, phone int,foreign key (phone) references t1(phone));
 -- insert into t3 values(1,134);
  -- drop table info2;
-- create table t1(id int primary key auto_increment,name varchar(20));
-- ALTER TABLE t1 AUTO_INCREMENT = (AUTO_INCREMENT%2=0);
 -- insert into t1 (name) values('c');
 --  select * from info2;
 -- create table info(name varchar(20),city varchar(20) default 'delhi');
 -- insert into info (name) values('C');
 -- insert into info values('D','Chennai');
--  create table info2(id int,result enum('@','#'));
-- insert into info2 values(4,'as@s');
-- drop table test;
-- select * from info;
-- insert into info values('E','bangalore'),('F','Pune'),('G','Mumbai');
-- update info set name='X'  where city='Pune';
-- delete from info where name='X';
-- savepoint p1;
-- insert into info values('K','kolkata');
-- delete from info where city='chennai';
-- update info set city='goa' where name='C'; 
select * from info;
rollback to p3;
-- create view emp as (select last_name,salary,hire_date from employees where manager_id is not null);
-- select dname from emp1 where ename ='king';
 -- SHOW FULL TABLES IN izmo WHERE TABLE_TYPE LIKE 'VIEW';
 -- create view emp1 as (select e.last_name as ename,d.department_name as dname from employees e inner join departments d on e.department_id=d.department_id order by 1 desc);
-- drop view emp1;
-- create view emp2 as(select salary,commission_pct from employees); 

truncate table dept;
select * from dept;
alter table dept add city varchar(200) not null;
alter table dept modify city varchar(20);
alter table dept add city varchar(200) first;
alter table dept drop city;
alter table dept add city varchar(200) after dept_id;
alter table dept change column city  dept_city varchar(200) after dept_id;
alter table dept rename to mydept;
*/
-- alter table info rename to infomation;
-- desc info;
-- alter table infomation rename to info;
-- alter table info add age int after name;
select * from info;
-- alter table info add country varchar(100) first;
-- alter table info drop column age;
-- alter table info modify column city varchar (40);
desc info;
-- alter table info modify column country varchar (50);
desc dept;
alter table info modify name varchar(30) not null;
-- alter table info modify column name varchar (30) unique;
-- delete from info where name in('E','G');
-- insert into info values('india','H','pune');
-- alter table info add id int ;
select * from info;
-- update info set id=3 where name='H';
-- alter table info add constraint primary key pk ()
-- alter table info drop id;
-- alter table info add id int
-- alter table info add constraint foreign key(id) references dept(did);
